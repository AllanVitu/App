{
  "name": "saas-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Parcours de bout en bout — exécutés hors conteneur : Playwright ne distribue pas de binaires pour Alpine (musl).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
